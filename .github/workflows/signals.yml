name: Henry Hub Signals

on:
  push:
    branches: [ "main" ]
  schedule:
    - cron: "0 9 * * *"  # каждый день в 09:00 UTC

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      # ВАЖНО: имя переменной и секрет должны быть строго на латинице
      EIA_API_KEY: \${{ secrets.EIA_API_KEY }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: pip install -r requirements.txt

      # ВРЕМЕННЫЙ ШАГ ДЛЯ ОТЛАДКИ: проверка, виден ли ключ
      - name: DEBUG: Check EIA Key presence
        run: |
          if [ -n "\$EIA_API_KEY" ]; then
            echo "✅ EIA_API_KEY FOUND (value hidden by GitHub)"
          else
            echo "❌ ERROR: EIA_API_KEY is EMPTY"
          fi

      - name: Run signal script
        run: python henry_hub_signals.py

